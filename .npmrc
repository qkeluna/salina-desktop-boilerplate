# pnpm configuration
shamefully-hoist=true
strict-peer-dependencies=false

# Windows build tools configuration (if available)
# Uncomment these lines if you have Visual Studio installed
# msvs_version=2022
# python=python

# better-sqlite3 build configuration
# Force rebuild for Electron
build-from-source=true

# Electron rebuild configuration
# This ensures native modules are built for Electron's Node.js version
electron-rebuild=true
